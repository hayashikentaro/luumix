{
  "name": "luumix",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/analysis",
    "packages/metadata"
  ],
  "scripts": {
    "analyze": "npm --workspace @luumix/analysis run analyze --",
    "build": "npm run build --workspace @luumix/metadata && npm run build --workspace @luumix/analysis",
    "init-overrides": "npm --workspace @luumix/analysis run init-overrides --",
    "report": "npm --workspace @luumix/analysis run report --",
    "resolve": "npm --workspace @luumix/analysis run resolve --",
    "test": "npm run build --workspace @luumix/metadata && npm run test --workspaces --if-present",
    "typecheck": "npm run build --workspace @luumix/metadata && npm run typecheck --workspace @luumix/analysis"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
